rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ВАЖНО: Правила для разработки. Разрешаем чтение и запись всем авторизованным.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}